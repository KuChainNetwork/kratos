package simulation

import "github.com/cosmos/cosmos-sdk/x/simulation"

type (
	AppParams         = simulation.AppParams
	WeightedOperation = simulation.WeightedOperation
	Params            = simulation.Params
)
